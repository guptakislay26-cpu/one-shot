import { NestFactory } from '@nestjs/core';import { ValidationPipe } from '@nestjs/common';import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';import { AppModule } from './app.module';
async function bootstrap(){const app=await NestFactory.create(AppModule);app.useGlobalPipes(new ValidationPipe({whitelist:true,forbidNonWhitelisted:true,transform:true}));if(process.env.NODE_ENV==='development'){const doc=SwaggerModule.createDocument(app,new DocumentBuilder().setTitle('FlyPal MRO API').build());SwaggerModule.setup('/api/docs',app,doc);}await app.listen(process.env.PORT??3000);}void bootstrap();
