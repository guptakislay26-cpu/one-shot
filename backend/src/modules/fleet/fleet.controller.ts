import { Body, Controller, Get, Param, Patch, Post, Query } from '@nestjs/common';import { Permissions } from '../../common/decorators/permissions.decorator';import { CurrentUser } from '../../common/decorators/current-user.decorator';import { FleetService } from './fleet.service';
@Controller('api/fleet') export class FleetController{constructor(private service:FleetService){}@Get()@Permissions('fleet.read')list(@Query()q:any){return this.service.list(q);}@Post()@Permissions('fleet.write')create(@Body()dto:any,@CurrentUser()u:any){return this.service.create(dto,u);}@Patch(':id')@Permissions('fleet.write')update(@Param('id')id:string,@Body()dto:any,@CurrentUser()u:any){return this.service.update(id,dto,u);}}
