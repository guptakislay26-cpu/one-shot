import { Injectable, Logger } from '@nestjs/common';@Injectable() export class FleetService{private readonly logger=new Logger(FleetService.name);list(q:any){this.logger.debug('List fleet');return {items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}create(dto:any,user:any){return {...dto,createdBy:user?.id};}update(id:string,dto:any,user:any){return {id,...dto,updatedBy:user?.id};}}
