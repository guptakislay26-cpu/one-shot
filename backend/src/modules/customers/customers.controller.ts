import { Body, Controller, Get, Param, Post, Query } from '@nestjs/common';import { Permissions } from '../../common/decorators/permissions.decorator';import { CustomersService } from './customers.service';
@Controller('api/customers') export class CustomersController{constructor(private service:CustomersService){}@Get()@Permissions('customers.read')list(@Query()q:any){return this.service.list(q);}@Post()@Permissions('customers.write')create(@Body()dto:any){return this.service.create(dto);}@Post(':id/rate-cards')@Permissions('customers.write')addRateCard(@Param('id')id:string,@Body()dto:any){return this.service.addRateCard(id,dto);}}
