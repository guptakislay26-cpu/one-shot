import { Injectable, Logger } from '@nestjs/common';@Injectable() export class CustomersService{private readonly logger=new Logger(CustomersService.name);list(q:any){return {items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}create(dto:any){this.logger.log(`Create customer ${dto.name}`);return dto;}addRateCard(customerId:string,dto:any){return {customerId,...dto};}}
