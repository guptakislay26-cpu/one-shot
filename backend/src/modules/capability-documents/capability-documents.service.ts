import { Injectable, Logger } from '@nestjs/common';@Injectable()export class CapabilityDocumentsService{private readonly logger=new Logger(CapabilityDocumentsService.name);list(q:any){this.logger.debug('List capability-documents');return {feature:'capability-documents',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
