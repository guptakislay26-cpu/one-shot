import { Injectable, Logger } from '@nestjs/common';@Injectable()export class ReliabilityService{private readonly logger=new Logger(ReliabilityService.name);list(q:any){this.logger.debug('List reliability');return {feature:'reliability',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
