import { Injectable, Logger } from '@nestjs/common';@Injectable()export class InventoryService{private readonly logger=new Logger(InventoryService.name);list(q:any){this.logger.debug('List inventory');return {feature:'inventory',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
