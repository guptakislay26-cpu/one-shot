import { Injectable, Logger } from '@nestjs/common';@Injectable()export class SalesService{private readonly logger=new Logger(SalesService.name);list(q:any){this.logger.debug('List sales');return {feature:'sales',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
