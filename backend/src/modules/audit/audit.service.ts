import { Injectable, Logger } from '@nestjs/common';@Injectable()export class AuditService{private readonly logger=new Logger(AuditService.name);list(q:any){this.logger.debug('List audit');return {feature:'audit',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
