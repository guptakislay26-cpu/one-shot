import { Injectable, Logger } from '@nestjs/common';@Injectable()export class LineMaintenanceService{private readonly logger=new Logger(LineMaintenanceService.name);list(q:any){this.logger.debug('List line-maintenance');return {feature:'line-maintenance',items:[],page:q?.page??1,pageSize:Math.min(q?.pageSize??25,100)};}}
