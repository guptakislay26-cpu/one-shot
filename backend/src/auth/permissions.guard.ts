import { CanActivate, ExecutionContext, ForbiddenException, Injectable } from '@nestjs/common';import { Reflector } from '@nestjs/core';import { PERMISSIONS_KEY } from '../common/decorators/permissions.decorator';
@Injectable() export class PermissionsGuard implements CanActivate{constructor(private r:Reflector){} canActivate(ctx:ExecutionContext){const required=this.r.getAllAndOverride<string[]>(PERMISSIONS_KEY,[ctx.getHandler(),ctx.getClass()])??[];const user=ctx.switchToHttp().getRequest().user;if(!required.length||user?.role==='SUPER_ADMIN'||user?.permissions?.includes('*'))return true; if(required.every((p)=>user?.permissions?.includes(p)))return true; throw new ForbiddenException('Missing permission');}}
